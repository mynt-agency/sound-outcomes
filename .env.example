# HubSpot Forms — both lead forms submit here.
# These are PUBLIC values (they appear in any HubSpot embed code), so it's safe
# to expose them to the browser. Set them in Vercel: Project → Settings →
# Environment Variables (Production + Preview), then redeploy.

NEXT_PUBLIC_HUBSPOT_PORTAL_ID=
NEXT_PUBLIC_HUBSPOT_FORM_GUID=
